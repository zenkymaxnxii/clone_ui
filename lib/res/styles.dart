import 'package:flutter/material.dart';

class AppStyles {
  static var inputFieldBorder = OutlineInputBorder(
    borderRadius: BorderRadius.circular(10),
    borderSide: const BorderSide(color: Colors.transparent),
  );
}
